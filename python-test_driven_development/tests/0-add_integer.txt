===========================
How to Use 0-add_integer.py
===========================

Description
===========

This module defines an integer addition function ``add_integer(a, b=98)``.

Usage
=====

``add_integer(...)``` returns the addition of its two arguments. For numbers,
that value is equivalent to using the ``+`` operator.

::

    >>> add_integer = __import__('your_file_name').add_integer
    >>> add_integer(1, 2)
    3

::

    >>> add_integer(1, -2)
    -1

::

    >>> add_integer(1.4, 2)
    3

::

    >>> add_integer(1, "2")
    Traceback (most recent call last):
    TypeError: b must be an integer

Regular tests passed

Handled Edge Cases:
- The function correctly raises a TypeError when a non-integer or non-float is provided as the first argument.
- The function correctly raises a TypeError when a non-integer or non-float is provided as the second argument.
- The function correctly handles the case when None is provided as the first argument.
- The function correctly handles the case when None is provided as the second argument.
- The function correctly handles the case when both arguments are non-integer and non-float, raising a TypeError only for the first argument.
- The function correctly handles the case when float('inf') is provided, raising an OverflowError.
- The function correctly handles the case when float('nan') is provided, raising a ValueError.
